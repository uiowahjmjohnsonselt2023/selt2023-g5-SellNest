<!DOCTYPE html>
<html>
<head>
  <title>Create Listing</title>
  <link rel="stylesheet" href="styles.css"> <!-- Link to your CSS stylesheet for styling -->
</head>
<body>
<div class="container">
  <h1>Create Listing</h1>
  <%= form_for(@listing, url: new_listing_path, html: {class: "form-horizontal"}) do |f| %>
    <div class="form-group">
      <% f.label :name %>
      <% f.name_field :name, class: 'form-control' %>
    </div>

    <div class="form-group">
      <label for="item_photos">Upload Photos:</label>
      <input type="file" id="item_photos" name="item_photos" accept="image/*" multiple>
    </div>

    <div class="form-group">
      <% f.label :description %>
      <% f.description_field :name, class: 'form-control' %>
    </div>

    <div class="form-group">
      <% f.label :price %>
      <% f.name_field :price, class: 'form-control' %>
    </div>

    <div class="form-group">
      <input type="submit" value="Submit Listing">
    </div>
  <% end %>>
</div>
</body>
</html>
