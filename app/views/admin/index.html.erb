<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
  <style>
      body {
          font-family: 'Arial', sans-serif;
          background-color: #f7f7f7;
          margin: 0;
          padding: 0;
      }

      .dashboard-header {
          background: #fff;
          padding: 20px 0;
          border-bottom: 2px solid #eaeaea;
          text-align: center;
      }

      .dashboard-stats {
          display: grid;
          grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
          gap: 1rem;
          margin-top: 2rem;
      }

      .stat-card {
          background: #fff;
          border: 1px solid #eaeaea;
          border-radius: 8px;
          padding: 20px;
          text-align: center;
          transition: transform 0.3s ease-in-out;
      }

      .stat-card:hover {
          transform: translateY(-5px);
          box-shadow: 0 5px 15px rgba(0,0,0,0.1);
      }

      .stat-title {
          font-size: 1.5rem;
          color: #333;
          margin: 10px 0;
      }

      .stat-value {
          font-size: 2.5rem;
          font-weight: bold;
          color: #333;
      }

      .container {
          max-width: 1200px;
          margin: auto;
          padding: 0 15px;
      }

      .text-logo {
          display: block;
          text-align: center;
          font-size: 24px;
          font-weight: bold;
          color: #ffd700;
          text-decoration: none;
          margin-bottom: 20px;
      }

      /* Improved styling for the container */
      .container {
          background-color: #fff;
          border-radius: 8px;
          box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
          margin-top: 20px;
          overflow: hidden;
      }

      /* Improved styling for the headings inside the container */
      .container h2 {
          background-color: #333;
          color: #fff;
          padding: 10px;
          margin: 0;
      }

      /* Improved styling for the lists inside the container */
      .container ul {
          list-style: none;
          padding: 0;
          margin: 0;
      }

      /* Improved styling for list items inside the container */
      .container li {
          border-bottom: 1px solid #eaeaea;
          padding: 10px;
          display: flex;
          justify-content: space-between;
          align-items: center;
      }

      /* Styling for buttons inside the list items */
      .container li button {
          background-color: #dc3545;
          color: #fff;
          border: none;
          padding: 5px 10px;
          cursor: pointer;
      }

      /* Adjusted styling for the ban/remove buttons */
      .container li button.btn-danger {
          background-color: #dc3545;
      }

      /* Hover effect for list items */
      .container li:hover {
          background-color: #f7f7f7;
      }
  </style>
</head>
<body>
<div class="container">
  <div class="dashboard-header">
    <a href="/" class="text-logo">SellNest</a>
    <h1>Admin Dashboard</h1>
    <p>Quick stats overview</p>
  </div>

  <div class="dashboard-stats">

    <div class="stat-card">
    <h2 class="stat-title">Total Sales</h2>
      <p class="stat-value"><%= number_to_currency(@total_sales) %></p>
    </div>

    <div class="stat-card">
      <h2 class="stat-title">Listed Merchandise</h2>
      <p class="stat-value"><%= number_to_currency(@listed_merchandise) %></p>
    </div>

    <div class="stat-card">
      <h2 class="stat-title">Total Users</h2>
      <p class="stat-value"><%= @total_users %></p>
    </div>

    <div class="stat-card">
      <h2 class="stat-title">Total Sellers</h2>
      <p class="stat-value"><%= @total_sellers %></p>
    </div>

    <div class="stat-card">
      <h2 class="stat-title">Items Listed</h2>
      <p class="stat-value"><%= @items_listed %></p>
    </div>

    <div class="stat-card">
      <h2 class="stat-title">Active Listings</h2>
      <p class="stat-value"><%= @active_listings %></p>
    </div>

    <div class="stat-card">
      <h2 class="stat-title">Sold Listings</h2>
      <p class="stat-value"><%= @sold_listings %></p>
    </div>

    <div class="stat-card">
      <h2 class="stat-title">Company Profits (5%)</h2>
      <p class="stat-value"><%= number_to_currency(@company_profits) %></p>
    </div>

    <div class="container">
      <h2>All Users</h2>
      <ul>
        <% @all_users.each do |user| %>
          <% unless user.email == 'admin@email' %>
            <li><%= user.email %> (ID: <%= user.id %>)
            <%= button_to "Ban", user_path(user), method: :delete, data: { confirm: "Are you sure you want to ban this user?" }, class: "btn btn-danger", style: "margin-left: 10px;" %>
            </li>
          <% end %>
        <% end %>
      </ul>
    </div>

    <div class="container">
      <h2>All Listings</h2>
      <ul>
        <% @all_listings.each do |listing| %>
          <li><%= listing.name %> (ID: <%= listing.id %>)
          <%= button_to "Remove", listing_path(listing), method: :delete, data: { confirm: "Are you sure you want to delete this listing?" }, class: "btn btn-danger", style: "margin-left: 10px;" %>
          </li>
        <% end %>
      </ul>
    </div>

    <div class="container">
      <h2>All Orders</h2>
      <ul>
        <% @all_orders.each do |order| %>
          <li><%= order.total %> (ID: <%= order.id %>)
            <%= button_to "Refund", refund_order_path(order), method: :post, data: { confirm: "Are you sure you want to refund this order?" }, class: "btn btn-danger", style: "margin-left: 10px;" %>
          </li>
        <% end %>
      </ul>
    </div>

  </div>
</div>
</body>
</html>
